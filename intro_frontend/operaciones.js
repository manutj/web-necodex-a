   export const sumar = (a, b) => a + b  
   export const restar = (a, b) => a - b 

 const producto = (a, b) => a * b


  //sumar(3,5)



export {
    sumar as suma,
    restar as resta
}
  
  export default producto
